// Dev-only shortcut credentials — never used in production builds (gated by
// __DEV__, which Metro strips to `false` and dead-code-eliminates in release
// builds). Auto-registered on first use if the account doesn't exist yet.
export const DEV_EMAIL = 'dev@example.com';
export const DEV_PASSWORD = 'dev-password-123';
